-r requirements.txt

# pytest 9 runs both the existing unittest.TestCase suite (including subtests)
# and function-style regression tests that unittest discovery cannot collect.
pytest==9.1.1
pip-audit==2.10.1
boto3-stubs[s3]==1.43.51
