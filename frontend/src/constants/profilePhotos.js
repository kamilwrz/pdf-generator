/**
 * Shared profile-photo library limits for the editor gallery and upload surface.
 *
 * Must stay aligned with backend `MAX_IMAGES_PER_USER` (default 4). The gallery
 * always renders this many slots so empty placeholders communicate capacity.
 */
export const MAX_PROFILE_PHOTOS = 4;
