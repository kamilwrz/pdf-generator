/**
 * Shared profile-photo library limits for the editor gallery and upload dialog.
 *
 * Must stay aligned with backend `MAX_IMAGES_PER_USER` (default 5). The gallery
 * always renders this many slots so empty placeholders communicate capacity.
 */
export const MAX_PROFILE_PHOTOS = 5;
