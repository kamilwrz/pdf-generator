import fs from 'node:fs';
const root = 'C:/Users/Kamil/learningCode/PROJECTS/PDF/pdf-generator/';
const component = 'frontend/src/components/editor/NewCvSetupModal/NewCvSetupModal.jsx';
const styles = 'frontend/src/components/editor/NewCvSetupModal/NewCvSetupModal.module.css';
const e2e = 'frontend/e2e/new-cv-setup.spec.js';
const read = path => fs.readFileSync(root + path, 'utf8');
const count = path => read(path).trimEnd().split('\n').length;
let readme = read('README.md');
const english = '**Dostosuj zawartość** replaces the gallery with one settings view at a time: **Nagłówek i kontakt** or **Sekcje CV**. **Szablony** returns to the gallery without losing selections or unfinished custom-section input. Three compact template rows show an A4 thumbnail, column/photo metadata, and an explicit selected indicator; **Więcej szablonów** switches to a compact two-column gallery with Pro locks. The selected sample appears once, at a viewport-dependent size that preserves its A4 ratio, and is explicitly labelled as sample data. Contact choices retain the always-present name, optional social-link disclosure, and template-dependent photo control. Switching to an incompatible template clears the photo and announces why. Sections use a two-column, row-major ordered list on desktop, with 44px up/down controls, drag-and-drop, and an optional custom-section field. On compact screens section rows form one column. The default gallery and ordinary desktop settings fit without scrolling at the tested laptop and wide sizes. The shared dialog body remains the only scroll fallback for short screens, 200% zoom, errors, and additional custom sections; it never hides controls to force a fit. Below 900px, **Podgląd szablonu** temporarily replaces the form while keeping its toggle and primary action reachable. Closing the sample restores the same form state. Loading and failed images retain their A4 footprint and never block creation. The contact view contains the configuration summary; empty section validation switches to the sections view and focuses its labelled error.';
const polish = '**Dostosuj zawartość** zastępuje galerię jednym widokiem ustawień naraz: **Nagłówek i kontakt** albo **Sekcje CV**. **Szablony** przywraca galerię bez utraty wyborów i niedokończonej nazwy sekcji własnej. Trzy zwarte wiersze szablonów pokazują miniaturę A4, informacje o kolumnach i zdjęciu oraz jawny znacznik wyboru; **Więcej szablonów** przełącza na zwartą galerię dwukolumnową z blokadami Pro. Wybrana próbka pojawia się tylko raz, w rozmiarze zależnym od viewportu zachowującym proporcje A4, i jest jednoznacznie opisana jako przykładowe dane. Kontakty zachowują zawsze obecne imię i nazwisko, opcjonalne rozwinięcie linków oraz zdjęcie zależne od szablonu. Wybór niezgodnego szablonu wyłącza zdjęcie i ogłasza przyczynę. Sekcje tworzą na desktopie uporządkowaną listę dwukolumnową czytaną wierszami, z przyciskami góra/dół 44px, przeciąganiem i polem sekcji własnej. Na małych ekranach wiersze sekcji układają się w jednej kolumnie. Domyślna galeria i zwykłe ustawienia desktopowe mieszczą się bez przewijania w sprawdzanych rozmiarach laptopowych i szerokich. Treść wspólnego dialogu pozostaje jedynym awaryjnym obszarem przewijania dla niskich ekranów, zoomu 200%, błędów i dodatkowych sekcji własnych; kontrolki nie są ukrywane w celu wymuszenia dopasowania. Poniżej 900px **Podgląd szablonu** tymczasowo zastępuje formularz, zachowując dostępny przełącznik i akcję główną. Zamknięcie próbki przywraca ten sam stan formularza. Ładowanie i błędy obrazów zachowują geometrię A4 i nie blokują tworzenia. Widok kontaktu zawiera podsumowanie konfiguracji; walidacja braku sekcji przełącza na widok sekcji i fokusuje podpisany błąd.';
readme = readme.replace(/^\*\*Dostosuj zawartość\*\* is optional[^\r\n]*/m, english);
readme = readme.replace(/^\*\*Dostosuj zawartość\*\* jest opcjonalne[^\r\n]*/m, polish);
for (const path of [component, styles, e2e]) {
  const escaped = path.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  readme = readme.replace(new RegExp('`' + escaped + '`, (lines|linie) [0-9]+–[0-9]+', 'g'), (_, lang) => '`' + path + '`, ' + lang + ' 1–' + count(path));
}
readme = readme.replace('— siatka wyboru szablonu w konfiguratorze A4', '— komponent `NewCvSetupModal`, w tym zwarta galeria wyboru szablonu w konfiguratorze A4');
const noteEn = `\n\nViewport regression: \`${e2e}\`, lines 1–${count(e2e)}, verifies the default gallery and contact view at 390×844, 834×950, 1280×720, 1366×768, and 1920×920, plus the full gallery and section view without scrolling on desktop. The same suite checks focus trapping, choice retention, error/retry, creation, cancellation, reduced motion, and print exclusion. [MDN viewport lengths](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/length) explains the dynamic viewport units used to size the sample without changing PDF dimensions.`;
const notePl = `\n\nRegresja viewportu: \`${e2e}\`, linie 1–${count(e2e)}, sprawdza domyślną galerię i kontakt przy 390×844, 834×950, 1280×720, 1366×768 i 1920×920 oraz pełną galerię i sekcje bez przewijania na desktopie. Ten sam zestaw sprawdza pułapkę fokusu, zachowanie wyborów, błąd/ponowienie, tworzenie, anulowanie, ograniczenie ruchu i wykluczenie z druku. [Jednostki viewportu w MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/length) wyjaśniają dynamiczne jednostki używane do skalowania próbki bez zmiany wymiarów PDF.`;
readme = readme.replace(english, english + noteEn).replace(polish, polish + notePl);
fs.writeFileSync(root + 'README.md', readme);
let design = read('DESIGN.md');
design = design.replace('Header/contact and ordered sections are optional disclosures under Customize content;', 'Customize content replaces the gallery with switchable header/contact and ordered-section views;');
design = design.replace('Keep the configuration summary inside optional settings. Desktop shows a labelled sample preview; compact layouts expose the sample on demand and keep the main action reachable.', 'Keep the configuration summary in the contact settings. Use compact selectable template rows and a single labelled A4 sample sized to the viewport. Default setup and ordinary settings fit laptop and wide desktop viewports without scrolling; sections use two columns in row-major order with 44px reorder controls. Compact layouts expose the sample on demand in place of the form and keep its toggle and the main action reachable. Preserve the shared body scroll fallback for short screens, 200% zoom, errors and additional custom sections instead of clipping content.');
fs.writeFileSync(root + 'DESIGN.md', design);
console.log('Updated bilingual setup documentation and design contract; source lengths:', count(component), count(styles), count(e2e));
